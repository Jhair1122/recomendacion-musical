# Conexión a Supabase (variables que definiremos en Render)
spring.datasource.url=${jdbc:postgresql://db.supkfiqakidxzkmrowyo.supabase.co:5432/postgres?sslmode=require}
spring.datasource.username=${Jhair1122}
spring.datasource.password=${StreamFlix2025!}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

server.port=${PORT:8080}
